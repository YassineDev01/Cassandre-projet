<header class="navbar">
    <div class="navbar-brand">Cassandre System</div>

    <nav class="navbar-menu">
        <a href="{{ path('app_accueil') }}" class="navbar-link">Accueil</a>
        <a href="{{ path('app_apropos') }}" class="navbar-link">A propos</a>

        <button type="button" onclick="window.location.href='{{ path('admin') }}'" class="btn btn-success">
            Dashboard Admin
        </button>

      <button type="button" onclick="window.location.href='{{ path('app_login') }}'" class="btn btn-primary">
    Connexion
</button>

    </nav>
</header>


